---
name: Bug report
about: Report a problem with ServerTools
labels: bug
---

**Version**
Output of `bash server-tools.sh --version` (or the legacy script name/version):

**Environment**
- Distro (e.g. Ubuntu 24.04):
- Virtualization (KVM / LXC / OpenVZ / bare metal):
- Workload on the server (marzban-node / pg-node / panel / wireguard / none):

**What happened**

**What you expected**

**Log**
Attach the relevant part of `/var/log/server-tools.log` and, if useful,
the output of `bash server-tools.sh --status --no-color`.
